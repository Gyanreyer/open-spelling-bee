<input webc:root="override" id="guess-input" type="text"
  x-model="guess"
  x-on:input="guess = $event.target.value.replace($store.game.invalidWordRegex, '')"
></input>
<style webc:scoped>
  :host {
    font-size: 2rem;
    text-transform: uppercase;
    text-align: center;

    background: none;
    border: none;
  }
</style>
